
<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>
  <body class='<%= "#{controller_name}" + " " + "#{controller_name}" + "-" + "#{action_name}" %>'>
    <%= render 'spree/shared/google_analytics.js' %>
    <%= yield %>

    <%= react_component 'HelloWorldContainer', { name: 'SOMEONE' }, { prerender: false } %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
