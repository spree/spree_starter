databases:
  - name: spree_legacy_frontend_db
    databaseName: spree
    user: spree
    region: frankfurt

services:
  - type: web
    name: spree_legacy_frontend_web
    env: ruby
    region: frankfurt
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: spree_legacy_frontend_db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: spree_legacy_frontend_redis
          property: connectionString
      - key: SECRET_KEY_BASE
        generateValue: true
      - key: ADMIN_EMAIL
        value: 'spree@example.com'
      - key: ADMIN_PASSWORD
        value: 'spree123'
  - type: redis
    name: spree_legacy_frontend_redis
    region: frankfurt
    ipAllowList: [] # only allow internal connections


